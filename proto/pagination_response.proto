syntax = "proto3";

package pb;

import "validate/validate.proto";

option go_package = "github.com/oopchi/gudang_buku_server/pb";

message PaginationResponse {
    option (validate.ignored) = true;

    int64 totalElements = 1;
    int32 currentPage = 2;
    int64 currentElements = 3;
}